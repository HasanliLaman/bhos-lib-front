import styled from "styled-components";

const StyleStarContainer = styled.div`
  display: flex;
  gap: 0.9rem;
  height: 20.56px;
  overflow: hidden;
  align-items: center;

  ul {
    display: flex;
    width: 75.05px;
    justify-content: space-between;
  }

  p {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--color-accent);
  }
`;
export default StyleStarContainer;

import styled from "styled-components";

const StyleCartContainer = styled.section`
  padding: 8rem 0 12rem;

  & > div {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }
`;

export default StyleCartContainer;

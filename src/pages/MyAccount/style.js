import styled from "styled-components";

const StyleMyAccount = styled.section`
  padding: 8rem 0 10rem;
  background: #ececec;

  & > div {
    display: flex;
    flex-direction: column;
    gap: 6rem;
  }
`;

export default StyleMyAccount;

import styled from "styled-components";

const StyleBookImage = styled.div`
  padding: 3rem;
  border-radius: 1.5rem;
  border: 1px solid var(--color-border);

  img {
    width: 100%;
  }
`;

export default StyleBookImage;
